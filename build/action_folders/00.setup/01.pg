#!/bin/bash

declare -ar env_pg=(
    'PGDATA="${PGDATA:-/var/lib/postgresql/data}"'
    'PG_UID=${PG_UID:-$(postgres.UID)}'
    'PG_GID=${PG_GID:-$(postgres.GID)}'
)

crf.updateRuntimeEnvironment "${env_pg[*]}" 

crf.copyFileToRuntime 'LIB' 'postgres.bashlib'
