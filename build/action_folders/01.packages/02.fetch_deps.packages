# core Packages

if [ "${CBF['os_id']}" = 'alpine' ]; then
    declare PKGS='ca-certificates openssl git tar'
    package.checkIfInstalled "$PKGS"
    apk add --no-cache --virtual .fetch-deps $PKGS
fi